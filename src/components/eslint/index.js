import React from 'react';

const ESLint = () => (
  <div>We can take the very popular airbnb eslint cfg and extend it. Some rules
    are pretty strict, with little gain for the avergage app.
  </div>
);

export default ESLint;
