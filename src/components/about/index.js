import React from 'react';

const About = () => (
  <div>Reusable components are what helps make React super
    cool to use. Beause a component is just a <code>function</code>,
    components can be tested in isolation, and reused wherever
    necessary. We can nest components deeply, and still test.
  </div>
);

export default About;
